export const getGameBoard = (state) => state.game.gameBoard;

export const getDifficultyLevel = (state) => state.game.difficultyLevel;

export const getNumberOfMines = (state) => state.game.numberOfMines;

export const getNumberOfRows = (state) => state.game.numberOfRows;

export const getNumberOfColumns = (state) => state.game.numberOfColumns;

export const getEllapsedTime = (state) => state.game.ellapsedTime;

export const getGameStatus = (state) => state.game.gameStatus;

export const getCurrentCell = (state) => state.game.currentCell;
